#!/bin/bash
set -euo pipefail

/usr/bin/python3 -m venv .venv_package
source .venv_package/bin/activate
rm -rf build dist || echo "" > /dev/null
pip install -r requirements.txt
pyinstaller --clean --onefile biograf.py
deactivate
rm -rf .venv_package